# 백엔드 배포 안내

## 개요

- 해당 백엔드 코드는 **AWS EC2**에 배포해야 하며, 데이터베이스는 **RDS**를 연결하여 사용한다.

## RDS 설정 관련 유의사항

- RDS 접속을 위해 반드시 **올바른 사용자명과 비밀번호**로 변경해야 한다.
- 사용자명과 비밀번호 변경은 프로젝트 내 **`.env` 파일과 `/src/config/database.js` 파일**에서 설정할 수 있다.

---

변경 후 반드시 EC2에 배포된 서버가 정상적으로 RDS에 연결되는지 확인해야 한다.
